# 📝 Домашнее задание  
**Тема: Адрес файла в сети Интернет**  
**Информатика, 7 класс**

Для каждой задачи запишите **последовательность цифр или букв**, кодирующую **адрес файла** в сети Интернет.

---

### **Задача 1**  
Файл `tree.png` находится на сервере `forest.org` и доступен по протоколу `http`.  
Фрагменты адреса закодированы буквами от А до Ж:

| Буква | Фрагмент |
|-------|----------|
| А     | `.png`   |
| Б     | `://`    |
| В     | `http`   |
| Г     | `forest` |
| Д     | `/`      |
| Е     | `.org`   |
| Ж     | `tree`   |

Запишите последовательность букв, кодирующую адрес файла.

---

### **Задача 2**  
Файл `list.doc` был выложен по адресу `https://school.ru/list.doc`.  
Позже его переместили в подкаталог `homework`.  
Фрагменты **нового** адреса закодированы цифрами от 1 до 8:

| Цифра | Фрагмент |
|-------|----------|
| 1     | `https`  |
| 2     | `://`    |
| 3     | `school` |
| 4     | `.ru`    |
| 5     | `/`      |
| 6     | `homework`|
| 7     | `list`   |
| 8     | `.doc`   |

Запишите последовательность цифр.

---

### **Задача 3**  
Файл `map.kml` изначально находился по адресу `http://geo.net/map.kml`.  
Его переместили:
- на сервер `earth.edu`,
- в каталог `maps`,
- и теперь доступ осуществляется по протоколу `ftp`.

Фрагменты **нового** адреса:

| Цифра | Фрагмент |
|-------|----------|
| 1     | `http`   |
| 2     | `ftp`    |
| 3     | `://`    |
| 4     | `geo`    |
| 5     | `.net`   |
| 6     | `earth`  |
| 7     | `.edu`   |
| 8     | `/`      |
| 9     | `maps`   |
| 10    | `map`    |
| 11    | `.kml`   |

Запишите последовательность цифр, соответствующую новому адресу.

---

### **Задача 4**  
Файл `report.pdf` был доступен по адресу `ftp://files.ru/report.pdf`.  
Позже его:
- переместили на сервер `docs.school72.ru`,
- поместили в структуру каталогов `reports/2025/`,
- и оставили протокол `ftp`.

Фрагменты нового адреса:

| Буква | Фрагмент     |
|-------|--------------|
| А     | `ftp`        |
| Б     | `://`        |
| В     | `files`      |
| Г     | `.ru`        |
| Д     | `docs`       |
| Е     | `school72`   |
| Ж     | `.ru`        |
| З     | `/`          |
| И     | `reports`    |
| К     | `2025`       |
| Л     | `report`     |
| М     | `.pdf`       |

Запишите последовательность букв.

---

### **Задача 5**  
Файл `photo.jpeg` находился по адресу `http://oldsite.com/photo.jpeg`.  
Теперь:
- сервер изменился на `gallery.museum.org`,
- файл переместили в каталог `exhibits/animals/`,
- протокол заменён на `https`.

Фрагменты нового адреса:

| Цифра | Фрагмент        |
|-------|-----------------|
| 1     | `http`          |
| 2     | `https`         |
| 3     | `://`           |
| 4     | `oldsite`       |
| 5     | `.com`          |
| 6     | `gallery`       |
| 7     | `museum`        |
| 8     | `.org`          |
| 9     | `/`             |
| 10    | `exhibits`      |
| 11    | `animals`       |
| 12    | `photo`         |
| 13    | `.jpeg`         |

Запишите последовательность цифр, кодирующую новый адрес.
