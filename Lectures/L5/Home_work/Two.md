# 📝 Домашнее задание  
**Тема: Адрес файла в сети Интернет**  
**Информатика, 7 класс**

Для каждой задачи запишите последовательность букв или цифр, кодирующую адрес указанного файла в сети Интернет.

---

### **Задача 1**  
Файл `cat.html` находится на сервере `web.info` и доступен по протоколу `http`.  
Фрагменты адреса закодированы буквами от А до Ж:

| Буква | Фрагмент |
|-------|----------|
| А     | `.html`  |
| Б     | `://`    |
| В     | `http`   |
| Г     | `web`    |
| Д     | `/`      |
| Е     | `.info`  |
| Ж     | `cat`    |

Запишите последовательность букв.

---

### **Задача 2**  
Файл `data.csv` был выложен по адресу `https://analytics.org/data.csv`.  
Позже его переместили в подкаталог `reports`.  
Фрагменты **нового** адреса закодированы цифрами от 1 до 8:

| Цифра | Фрагмент |
|-------|----------|
| 1     | `https`  |
| 2     | `://`    |
| 3     | `analytics`|
| 4     | `.org`   |
| 5     | `/`      |
| 6     | `reports`|
| 7     | `data`   |
| 8     | `.csv`   |

Запишите последовательность цифр.

---

### **Задача 3**  
Файл `image.png` изначально находился по адресу `http://oldsite.ru/image.png`.  
Его переместили:
- на сервер `cloud.edu`,
- в каталог `images`,
- и теперь доступ осуществляется по протоколу `https`.

Фрагменты нового адреса:

| Цифра | Фрагмент |
|-------|----------|
| 1     | `http`   |
| 2     | `https`  |
| 3     | `://`    |
| 4     | `oldsite`|
| 5     | `.ru`    |
| 6     | `cloud`  |
| 7     | `.edu`   |
| 8     | `/`      |
| 9     | `images` |
| 10    | `image`  |
| 11    | `.png`   |

Запишите последовательность цифр, соответствующую новому адресу.

---

### **Задача 4**  
Файл `notes.txt` был доступен по адресу `ftp://files.net/notes.txt`.  
Позже его:
- переместили на сервер `docs.school72.ru`,
- поместили в каталог `2025/september/`,
- и оставили протокол `ftp`.

Фрагменты нового адреса:

| Буква | Фрагмент     |
|-------|--------------|
| А     | `ftp`        |
| Б     | `://`        |
| В     | `files`      |
| Г     | `.net`       |
| Д     | `docs`       |
| Е     | `school72`   |
| Ж     | `.ru`        |
| З     | `/`          |
| И     | `2025`       |
| К     | `september`  |
| Л     | `notes`      |
| М     | `.txt`       |

Запишите последовательность букв.

---

### **Задача 5**  
Файл `exam.docx` находился по адресу `http://school.ru/exam.docx`.  
Теперь:
- сервер изменился на `edu.mos-school.net`,
- файл переместили в структуру каталогов `tests/grade7/informatics/`,
- протокол заменён на `https`.

Фрагменты нового адреса:

| Цифра | Фрагмент        |
|-------|-----------------|
| 1     | `http`          |
| 2     | `https`         |
| 3     | `://`           |
| 4     | `school`        |
| 5     | `.ru`           |
| 6     | `edu`           |
| 7     | `mos-school`    |
| 8     | `.net`          |
| 9     | `/`             |
| 10    | `tests`         |
| 11    | `grade7`        |
| 12    | `informatics`   |
| 13    | `exam`          |
| 14    | `.docx`         |

Запишите последовательность цифр.
