# 🌐 Адрес файла в сети Интернет  
**Информатика, 7 класс**

## 🎯 Цель урока

Научиться:
- понимать структуру URL-адреса файла в интернете,  
- правильно собирать полный адрес из фрагментов,  
- решать задачи на определение последовательности фрагментов, кодирующих адрес файла.

---

## 🔍 1. Основные понятия

### 📎 Что такое URL?
**URL (Uniform Resource Locator)** — это уникальный адрес ресурса (файла, страницы и т.д.) в интернете.

Полный адрес файла имеет следующую структуру:
протокол://доменное_имя/путь/имя_файла.расширение

### 🔤 Основные части URL

| Часть                | Пример         | Пояснение |
|----------------------|----------------|-----------|
| **Протокол**         | `http`, `https`, `ftp` | Способ передачи данных |
| **://**              | `://`          | Обязательный разделитель после протокола |
| **Имя сервера**      | `circ`         | Уникальное имя хоста (сервера) |
| **Доменная зона**    | `.org`, `.ru`, `.edu` | Указывает на тип организации или страну |
| **/**                | `/`            | Разделитель между доменом и путём к файлу |
| **Путь**             | `months`       | Каталог (папка) на сервере (может быть несколько уровней: `/a/b/c/`) |
| **Имя файла**        | `slon`         | Название файла без расширения |
| **Расширение**       | `.txt`, `.jpg` | Указывает тип файла |

> 💡 **Важно!**  
> - Имя файла **всегда в конце**.  
> - Каждый уровень пути разделяется **одним символом `/`**.  
> - Если файл переместили в подкаталог — в адрес добавляется ещё один фрагмент и ещё один `/`.

---

## 🧮 2. Пример разбора

**Задача**:  
Доступ к файлу `slon.txt`, находящемуся на сервере `circ.org`, осуществляется по протоколу `http`.  
Фрагменты адреса закодированы буквами от А до Ж. Запишите последовательность этих букв.

| Буква | Фрагмент |
|-------|----------|
| А     | `.txt`   |
| Б     | `://`    |
| В     | `http`   |
| Г     | `circ`   |
| Д     | `/`      |
| Е     | `.org`   |
| Ж     | `slon`   |

**Решение**:  
1. Собираем полный адрес:  
   `http://circ.org/slon.txt`  
2. Расставляем фрагменты в правильном порядке:  
   **http** → **://** → **circ** → **.org** → **/** → **slon** → **.txt**  
3. Соответствующие буквы:  
   **В** → **Б** → **Г** → **Е** → **Д** → **Ж** → **А**

✅ **Ответ**: **ВБГЕДЖА**

---

## ✍️ 3. Практика: Задачи на составление URL

### Задача 1 — Простой адрес  
Файл `may.jpeg` был выложен по адресу `http://spring.info/may.jpeg`.  
Потом его переместили в подкаталог `months`.  
Фрагменты **нового** адреса закодированы цифрами от 1 до 7:

| Цифра | Фрагмент |
|-------|----------|
| 1     | `http:/` |
| 2     | `spring` |
| 3     | `months` |
| 4     | `may`    |
| 5     | `.info`  |
| 6     | `/`      |
| 7     | `.jpeg`  |

> ⚠️ Обратите внимание: в фрагменте **1** уже есть **один слэш** (`http:/`), но в корректном URL нужно `http://`.  
> Однако в подобных задачах **`http:/`** часто означает **`http://`** (опечатка в условии — стандартная ловушка).  
> Считаем, что **1 = `http://`**.

**Решение**:  
Новый адрес: `http://spring.info/months/may.jpeg`  
→ 1 + 2 + 5 + 6 + 3 + 6 + 4 + 7

✅ **Ответ**: **12563647**

---

### Задача 2 — Смена сервера и протокола  
Файл `pig.pdf` сначала находился по адресу `http://mypigs.ru/pig.pdf`.  
Потом его:
- переместили на сервер `presentation.edu`,
- поместили в каталог `work`,
- и теперь доступ — по протоколу `ftp`.

Фрагменты нового адреса:

| Цифра | Фрагмент |
|-------|----------|
| 1     | `http:/` |
| 2     | `pig`    |
| 3     | `work`   |
| 4     | `presentation` |
| 5     | `.edu`   |
| 6     | `ftp:/`  |
| 7     | `/`      |
| 8     | `.pdf`   |
| 9     | `mypigs` |

**Решение**:  
Новый адрес: `ftp://presentation.edu/work/pig.pdf`  
→ 6 (`ftp:/` → считаем как `ftp://`) + 4 + 5 + 7 + 3 + 7 + 2 + 8

✅ **Ответ**: **64573728**

---

## 💡 Советы по решению

1. **Всегда сначала запишите полный URL** на черновике.
2. **Проверьте протокол**: `http`, `https`, `ftp`.
3. **Домен = имя + зона** (например, `circ` + `.org`).
4. **Каждый каталог — отдельный фрагмент**, между ними — `/`.
5. **Фрагмент `/` может использоваться несколько раз**, даже если дан один раз.
6. **Имя файла и расширение — всегда в самом конце**.

---

## 📊 Типичные ошибки

| Ошибка | Как избежать |
|-------|--------------|
| Перепутан порядок `.org` и `circ` | Помните: сначала имя сервера, потом доменная зона |
| Пропущен `/` перед именем файла | После домена всегда идёт `/`, даже если файл в корне |
| Неверный протокол (`http` вместо `ftp`) | Внимательно читайте условие! |
| Считают `http:/` за один слэш | В задачах это **всегда означает `http://`** |

> ✅ **Запомните схему**:  
> **протокол + :// + сервер + .зона + / + [каталоги/] + файл.расширение**
